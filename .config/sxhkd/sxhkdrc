#
# wm independent hotkeys
#

#
# program shortcuts
#

super + Return
  kitty -c ~/dotfiles/.config/kitty/kitty.conf \
        -c ~/dotfiles/.config/kitty/condensed.conf

super + shift + Return
  kitty -c ~/dotfiles/.config/kitty/kitty.conf \
        -c ~/dotfiles/.config/kitty/cozy.conf

super + alt + Return
  kitty -c ~/dotfiles/.config/kitty/kitty.conf \
        -c ~/dotfiles/.config/kitty/condensed.conf \
        tmux

super + alt + Tab
  kitty htop

# don't do this! it's taken by bspc restart
# super + alt + r
#   kitty ranger

super + alt + f
  thunar

super + alt + b
  vivaldi-stable

super + alt + d
  discord

# super + alt + m
#   pavucontrol
super + alt + m
  kitty pulsemixer

super + alt + w
  whatsdesk

super + a
  rofi -show run

XF86LaunchA
  rofi -show run

# there's an issue with polybar where it won't 
# lay over the active windows after first being launched.
# launching it (or trying to launch it) again fixes this.
super + x
  $HOME/dotfiles/.config/polybar/launch.sh & disown

super + z
  polybar-msg cmd toggle

super + Escape
  polybar-msg cmd toggle

super + shift + Escape
  sxhkd-reload

# kill X server
super + control + alt + Escape
  pkill Xorg

super + control + alt + Delete
  pkill Xorg

super + control + alt + l
  elliot logout

super + control + alt + z
  elliot suspend


# super + control + alt + t
#   ([[ $XORG_COLORSCHEME =~ "light" ]] && theme dark) || theme light

super + Delete
  elliot suspend

shift + XF86ScreenSaver
  elliot suspend

super + control + alt + s
  elliot shutdown

super + control + alt + i
  switch-mon internal

super + control + alt + o
  switch-mon external

super + control + alt + p
  switch-mon dual

XF86Search
  kitty ranger

# volume controls
XF86AudioRaiseVolume
  amixer -q sset Master 5%+

XF86AudioLowerVolume
  amixer -q sset Master 5%-

XF86AudioMute
  amixer -q sset Master 1+ toggle

super + F2
  amixer -q sset Master 5%-

super + F3
  amixer -q sset Master 5%+

# heylp! i forgot my other keymappings!
super + F4
  kitty nvim ~/dotfiles/.config/sxhkd/sxhkdrc 

# brightness
super + shift + F1
  blight set -5%

XF86MonBrightnessDown
  blight set -5%

super + shift + F2
  blight set +5%

XF86MonBrightnessUp
  blight set +5%

# touchpad shenanigans
super + F10
  tp off

super + F11
  tp on

# printscreen button
Print
  flameshot gui
