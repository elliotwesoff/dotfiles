# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
	name = Elliot Wesoff
	email = elliot.wesoff@gmail.com

# use meld as a difftool
[diff]
    tool = meld
[difftool]
    prompt = false
[difftool "meld"]
    cmd = meld "$LOCAL" "$REMOTE"
